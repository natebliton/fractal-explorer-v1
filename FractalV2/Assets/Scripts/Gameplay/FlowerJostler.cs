using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlowerJostler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /// <summary>
    /// When some other collider overlaps with the
    /// collider attached to the same GameObject
    /// as this Script, this method gets called
    /// </summary>
    /// <param name="coll"></param>
    private void OnTriggerEnter2D(Collider2D coll)
    {
        // enter code here to be run upon collision

    }
}
